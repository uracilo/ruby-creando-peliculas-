<p id="notice"><%= notice %></p>

<h1>Movies</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Tags</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.name %></td>
<td>
    <ul>
    <%  movie.tags.each do |tag| %>     
 <li>
        <%= tag.name  %> 
         <span class="badge  info"> 
         <%=  link_to "x", delete_tag_movie_path(movie,  tag_id: tag.id) %>
         </span>
</li>

        </li>
    <% end %> 
    </ul>
</td>

        <td class= "btn btn-primary"><%= link_to 'Show', movie %></td>
        <td class= "btn btn-warning"><%= link_to 'Edit', edit_movie_path(movie) %></td>
        <td class= "btn btn-danger"><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<br>

<%= link_to 'New Movie', new_movie_path %>
